import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true,
  
  // Disable error overlay if you prefer console-only errors
  // devIndicators: {
  //   buildActivityPosition: 'bottom-right',
  //   appIsrStatus: false,
  // },
}; 

export default nextConfig;
